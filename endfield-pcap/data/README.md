# Runtime parser data

Packet semantics and generated metadata are intentionally excluded. Point
`ENDFIELD_LOGS_DATA_ROOT` at a locally generated resource directory when running
features that require those indexes.

