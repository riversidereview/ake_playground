# Protocol source definitions

The complete extracted protocol source set is not redistributed here. The
minimal generated Python modules required by the current parser remain under
`src/endfield_pcap/proto_generated`.

