[project]
name = "uploader-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "httpx[socks]>=0.28.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

